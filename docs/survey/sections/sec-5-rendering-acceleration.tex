%% sec-5-rendering-acceleration.tex -- Rendering Acceleration

\section{Rendering Acceleration}
\label{sec:rendering}

We organise render-time acceleration into four directions.

\subsection{Rasterisation Optimisation}
Mobile-GS~\cite{du2026mobilegs} (Snapdragon~8~Gen~3, 127~FPS, Vulkan~2.0)
and Flux-GS~\cite{du2026fluxgs} (Snapdragon~8~Gen~3, 147~FPS @
2.1~MB indoor) define the current ceiling of mobile rasterisation;
both come from the Mobile-GS group. The reported numbers were measured
on Snapdragon~8~Gen~3 / Vulkan~2.0; we extrapolate to
Snapdragon~8~Gen~4 / Vulkan~1.3 in~\S\ref{sec:mobile} below.

\subsection{Ray-Tracing Trend}
4DGRT~\cite{liu20254dgrt} (NTU + Intel) introduces 4DGS ray tracing
and represents a high-fidelity future path. GS-NFS~\cite{ghosh2026gsnfs}
(NVIDIA Research) reaches 25~FPS decode for 4DGS on a Jetson~Orin
mobile GPU.

\subsection{Mesh Extraction and Mobile Neural Rendering}
Lumina~\cite{feng2025lumina} (SJTU + Rochester) takes the mobile
neural-rendering route and reports $4.5\times$ speed-up and
$5.3\times$ energy-efficiency improvement.

\subsection{On-Device Hardware Acceleration}
Neo~\cite{oh2025neo} proposes a Reuse-and-Update sorting accelerator
for on-device 3DGS rendering.

\subsection{Mobile Deployment Comparison (Table~\ref{tab:mobile})}
Table~\ref{tab:mobile} consolidates the on-device measurement
numbers reported across the surveyed mobile and edge-GPU works.
Note that Mobile-GS and Flux-GS were measured on
Snapdragon~8~Gen~3 / Vulkan~2.0; the project target is
Snapdragon~8~Gen~4 / Vulkan~1.3, which we extrapolate
from~\cite{oh2025neo}'s Reuse-and-Update accelerator trends.
\begin{table}[t]
\centering
\caption{Mobile / edge-GPU deployment comparison across the surveyed works. ``--'' = not reported.}
\label{tab:mobile}
\small
\begin{tabular}{lllllll}
\toprule
\textbf{Method} & \textbf{SoC} & \textbf{Res.} & \textbf{FPS} & \textbf{Model size} & \textbf{Energy} & \textbf{Vulkan} \\
\midrule
Mobile-GS~\cite{du2026mobilegs}       & Snap~8~Gen~3   & 1080p    & 127       & 2.1~MB         & --       & 2.0   \\
Flux-GS~\cite{du2026fluxgs}           & Snap~8~Gen~3   & 1080p    & 147       & 2.1~MB (indoor) & --      & 2.0   \\
Lumina~\cite{feng2025lumina}          & Mobile SoC     & 1080p    & --        & --              & $5.3\times$ less & -- \\
GS-NFS~\cite{ghosh2026gsnfs}          & Jetson~Orin    & 1080p    & 25 (decode) & --            & --       & --    \\
Neo~\cite{oh2025neo}                  & Mobile FPGA    & 1080p    & realtime    & --            & --       & --    \\
GaussLite~\cite{thomas2026gausslite}  & Snap-class edge & 720p     & realtime    & --            & --       & --    \\
Pocket-SLAM~\cite{li2026pocketslam}   & Snap-class     & 540p     & 30          & <50~MB        & --       & --    \\
TemporalGS~\cite{zhou2026temporalgs}  & desktop GPU    & 1080p    & speed-up    & plug-in       & --       & --    \\
\bottomrule
\end{tabular}
\end{table}

\bigskip
\noindent\textbf{Index of paper notes backing this section}:
\texttt{INDEX.md} \S C / D.
