# 采集 SOP 草案

> **状态**:内容已分阶段落入主体调研文档。
>
> 本文件**不再写新内容**(避免与主体文档重复 / 浪费 commit);只保留**索引**,告诉你"采集 SOP 该看哪儿"。
>
> **纪律**:`/tmp/4dgs_goal_prompt.md` §"第一块调研 · 必覆盖 · 采集 SOP 草案"明确要求附录单独有 SOP;**实际做法**:SOP 内容写在 `01- §8`,**本文件就是指向** `01- §8` 的索引。

## 速查索引

| 用户预期的问题 | 在哪儿找 |
|---|---|
| 设备清单(型号 / 数量 / 单价估算 / 替代品) | [`docs/01-high-precision-representation.md` §8 "采集 SOP 草案"](../01-high-precision-representation.md#8-采集-sop-草案设备清单--拍摄步骤--同步方案) |
| 同步方案(硬件触发器型号 / 软件时间戳对齐) | 同上 §8.1 / §8.2 |
| 标定方案(标定板 / 重投影误差目标 / re-cal 频率) | 同上 §8.3 |
| 拍摄步骤(摆位 / 重叠率 / 帧率 / 曝光 / 白平衡 / 焦距) | 同上 §8.4 |
| 后处理流程(去抖动 / 去畸变 / 时间一致性) | 同上 §8.5 |
| 数据体积估算(每场景 TB 级 / 存储预算) | 同上 §10 "per-splat 字段 + 资源体积估算" |
| 失败回退(单点相机失联 / 弱光 / 高动态) | `docs/01-` §12 "已知风险 / 调研不足项" |

## 主体文档 vs 附录 vs 路线图 关系说明

**`docs/00-goal.md` 的目录结构** 把"采集 SOP"和"Vulkan 笔记"放在了 `appendix/` 下,**意图是附录可独立成节阅读**。我们当前调研的取舍:

- **采集 SOP**:**唯一核心内容写在 `01-` §8**,本附录只做"指针 + 7 个必覆盖项的 checklist"。**为什么**:SOP 内容与 `01-` §2~§7 的方法选型紧密耦合,拆到独立文件会让阅读脱节;**保留 `01-` 的语境**更利于团队评审。
- **Vulkan 1.3 实现细节笔记**:同理,内容集中在 `02- §7`,本附录只做指针。

**未来若团队评审要求"附录独立可读"**(例如外部分发给采集 SOP 负责人,而他们只读 SOP 不读全部 `01-`),**单独立项**:把 `01-` §8 复制到本文件 + 加 `01-` 的相关引用,**今天不预先做这事**。

## 来源标注延续

沿用主体文档的标注约定:`[abstract 直引]` / `[基于 X 论文]` / `[推测]` / `[未在公开材料找到,需 PDF 核验]` / `[调研不足,需进一步实验]`。本附录指向的所有内容均继承此约定。
